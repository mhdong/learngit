git init
git add
git commit -m "commit XXX"
git status 随时掌握工作区的状态
git diff 查看修改内容
HEAD指向的版本就是当前版本，使用命令git reset --hard commit_id
git log查看提交历史 git log --pretty=oneline
git reflog查看命令历史，以便确定要回到未来的哪个版本
HEAD^ HEAD^^ HEAD~100
